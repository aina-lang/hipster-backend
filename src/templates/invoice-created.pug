doctype html
html
  head
    meta(charset="UTF-8")
    meta(name="viewport" content="width=device-width, initial-scale=1.0")
    style.
      * { margin: 0; padding: 0; box-sizing: border-box; }
      body { 
        font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
        line-height: 1.6; 
        color: #333;
        background-color: #f5f5f5;
      }
      .container { 
        max-width: 600px; 
        margin: 0 auto; 
        background-color: #ffffff;
      }
      .header { 
        background: linear-gradient(135deg, #4F46E5 0%, #7C3AED 100%);
        color: white; 
        padding: 40px 20px; 
        text-align: center;
      }
      .header h1 { 
        font-size: 28px; 
        font-weight: 700;
        margin-bottom: 8px;
      }
      .header p {
        font-size: 14px;
        opacity: 0.9;
      }
      .content { 
        padding: 40px 30px;
      }
      .greeting {
        font-size: 18px;
        font-weight: 600;
        margin-bottom: 16px;
        color: #1f2937;
      }
      .intro-text {
        color: #6b7280;
        margin-bottom: 24px;
        font-size: 15px;
      }
      .invoice-card { 
        background: #f9fafb;
        border: 2px solid #e5e7eb;
        border-radius: 12px;
        padding: 24px;
        margin: 24px 0;
      }
      .invoice-header {
        display: flex;
        justify-content: space-between;
        align-items: center;
        margin-bottom: 20px;
        padding-bottom: 16px;
        border-bottom: 2px solid #e5e7eb;
      }
      .invoice-ref {
        font-size: 16px;
        font-weight: 700;
        color: #1f2937;
      }
      .invoice-status {
        display: inline-block;
        padding: 6px 12px;
        border-radius: 6px;
        font-size: 12px;
        font-weight: 600;
        text-transform: uppercase;
        background-color: #dbeafe;
        color: #1e40af;
      }
      .invoice-details {
        margin: 16px 0;
      }
      .detail-row {
        display: flex;
        justify-content: space-between;
        padding: 8px 0;
        font-size: 14px;
      }
      .detail-label {
        color: #6b7280;
        font-weight: 500;
      }
      .detail-value {
        color: #1f2937;
        font-weight: 600;
      }
      .items-table {
        width: 100%;
        margin: 20px 0;
        border-collapse: collapse;
      }
      .items-table th {
        background-color: #f3f4f6;
        padding: 12px 8px;
        text-align: left;
        font-size: 12px;
        font-weight: 600;
        color: #6b7280;
        text-transform: uppercase;
        border-bottom: 2px solid #e5e7eb;
      }
      .items-table td {
        padding: 12px 8px;
        font-size: 14px;
        color: #1f2937;
        border-bottom: 1px solid #e5e7eb;
      }
      .items-table .text-right {
        text-align: right;
      }
      .total-section {
        margin-top: 16px;
        padding-top: 16px;
        border-top: 2px solid #e5e7eb;
      }
      .total-row {
        display: flex;
        justify-content: space-between;
        padding: 6px 0;
        font-size: 14px;
      }
      .total-row.final {
        font-size: 20px;
        font-weight: 700;
        color: #4F46E5;
        margin-top: 8px;
        padding-top: 12px;
        border-top: 2px solid #4F46E5;
      }
      .button-group {
        margin: 32px 0;
        text-align: center;
      }
      .button { 
        display: inline-block;
        background: #4F46E5;
        color: white !important;
        padding: 14px 32px;
        text-decoration: none;
        border-radius: 8px;
        font-weight: 600;
        font-size: 15px;
        margin: 8px;
        transition: background 0.3s ease;
      }
      .button:hover {
        background: #4338ca;
      }
      .button-secondary {
        background: #10b981;
      }
      .button-secondary:hover {
        background: #059669;
      }
      .button-outline {
        background: transparent;
        color: #4F46E5 !important;
        border: 2px solid #4F46E5;
      }
      .button-outline:hover {
        background: #f5f3ff;
      }
      .info-box {
        background: #eff6ff;
        border-left: 4px solid #3b82f6;
        padding: 16px;
        margin: 24px 0;
        border-radius: 6px;
      }
      .info-box p {
        margin: 0;
        color: #1e40af;
        font-size: 14px;
      }
      .footer { 
        text-align: center;
        padding: 30px 20px;
        background-color: #f9fafb;
        border-top: 1px solid #e5e7eb;
      }
      .footer p {
        color: #6b7280;
        font-size: 13px;
        margin: 4px 0;
      }
      .footer a {
        color: #4F46E5;
        text-decoration: none;
      }
      @media only screen and (max-width: 600px) {
        .content { padding: 24px 16px; }
        .invoice-card { padding: 16px; }
        .button { display: block; margin: 8px 0; }
        .invoice-header { flex-direction: column; align-items: flex-start; }
        .invoice-status { margin-top: 8px; }
      }
  body
    .container
      .header
        h1 💼 #{typeLabel || 'Document'}
        p Hipster Studio - Votre partenaire digital
      
      .content
        .greeting Bonjour #{clientName},
        
        p.intro-text Une nouvelle #{typeLabel ? typeLabel.toLowerCase() : 'facture'} a été créée pour vous. Vous trouverez ci-dessous tous les détails.
        
        .invoice-card
          .invoice-header
            .invoice-ref #{invoiceReference}
            .invoice-status #{status}
          
          .invoice-details
            if projectName
              .detail-row
                span.detail-label Projet
                span.detail-value #{projectName}
            
            if dueDate
              .detail-row
                span.detail-label Date d'échéance
                span.detail-value #{dueDate}
          
          if items && items.length > 0
            table.items-table
              thead
                tr
                  th Description
                  th.text-right Qté
                  th.text-right Prix Unit.
                  th.text-right Total
              tbody
                each item in items
                  tr
                    td #{item.description}
                    td.text-right #{item.quantity}
                    td.text-right #{item.unitPrice}€
                    td.text-right #{(item.quantity * item.unitPrice).toFixed(2)}€
          
          .total-section
            if subtotal
              .total-row
                span.detail-label Sous-total HT
                span.detail-value #{subtotal}€
            if tax
              .total-row
                span.detail-label TVA
                span.detail-value #{tax}€
            .total-row.final
              span Montant Total
              span #{amount}€
        
        if notes
          .info-box
            p 📝 #{notes}
        
        .button-group
          a.button(href=invoiceUrl) 📄 Voir le document
          br
          a.button.button-secondary(href=pdfDownloadUrl || invoiceUrl) ⬇️ Télécharger le PDF
          if mobileAppUrl
            br
            a.button.button-outline(href=mobileAppUrl) 📱 Ouvrir dans l'app
        
        p.intro-text Merci de votre confiance. Si vous avez des questions, n'hésitez pas à nous contacter.
      
      .footer
        p © 2025 Hipster Studio. Tous droits réservés.
        p 
          a(href="mailto:contact@hipster-studio.com") contact@hipster-studio.com
        p Cet email a été envoyé automatiquement, merci de ne pas y répondre directement.
