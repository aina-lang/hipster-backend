extends layout

block content
  div(style="margin-bottom: 32px;")
    h2 💼 #{typeLabel || 'Document'} disponible
    p Bonjour #{clientName}, une nouvelle #{typeLabel ? typeLabel.toLowerCase() : 'facture'} a été générée pour vous.

  div(style="background-color: #000000; border: 1px solid #1f1f1f; border-radius: 12px; padding: 24px; margin: 32px 0;")
    div(style="display: flex; justify-content: space-between; align-items: center; border-bottom: 1px solid #1f1f1f; padding-bottom: 16px; margin-bottom: 20px;")
      div(style="font-size: 18px; font-weight: 700; color: #ffffff;") #{invoiceReference}
      div(style="background-color: #1a1a1a; color: #a1a1aa; padding: 4px 12px; border-radius: 6px; font-size: 11px; text-transform: uppercase; font-weight: 600; border: 1px solid #262626;") #{status}
    
    div.details(style="font-size: 14px; color: #a1a1aa;")
      if projectName
        div(style="display: flex; justify-content: space-between; margin-bottom: 8px;")
          span Projet
          span(style="color: #ffffff; font-weight: 500;") #{projectName}
      if dueDate
        div(style="display: flex; justify-content: space-between;")
          span Échéance
          span(style="color: #ffffff; font-weight: 500;") #{dueDate}

    if items && items.length > 0
      table(style="width: 100%; border-collapse: collapse; margin: 24px 0;")
        thead
          tr
            th(style="text-align: left; font-size: 11px; color: #52525b; text-transform: uppercase; padding: 12px 0; border-bottom: 1px solid #1f1f1f;") Description
            th(style="text-align: right; font-size: 11px; color: #52525b; text-transform: uppercase; padding: 12px 0; border-bottom: 1px solid #1f1f1f;") Total
        tbody
          each item in items
            tr
              td(style="padding: 12px 0; border-bottom: 1px solid #0a0a0a; font-size: 14px; color: #d4d4d4;") #{item.description}
              td(style="text-align: right; padding: 12px 0; border-bottom: 1px solid #0a0a0a; font-size: 14px; color: #ffffff; font-weight: 500;") #{(item.quantity * item.unitPrice).toFixed(2)}€

    div(style="border-top: 1px solid #1f1f1f; padding-top: 16px; margin-top: 16px;")
      div(style="display: flex; justify-content: space-between; font-size: 14px; margin-bottom: 8px; color: #71717a;")
        span Sous-total HT
        span #{subtotal}€
      if tax
        div(style="display: flex; justify-content: space-between; font-size: 14px; margin-bottom: 8px; color: #71717a;")
          span TVA
          span #{tax}€
      div(style="display: flex; justify-content: space-between; font-size: 18px; font-weight: 700; color: #ffffff; margin-top: 12px; padding-top: 12px; border-top: 1px solid #ffffff;")
        span Total
        span #{amount}€

  if notes
    div(style="background-color: #0a0a0a; border-left: 3px solid #ffffff; padding: 16px; border-radius: 4px; margin: 24px 0;")
      p(style="margin: 0; font-size: 13px; color: #a1a1aa; font-style: italic;") 📝 Note: #{notes}

  div(style="text-align: center; margin-top: 32px;")
    a.btn(href=invoiceUrl style="margin: 8px;") 📄 Consulter en ligne
    | &nbsp;&nbsp;
    a.btn(href=pdfDownloadUrl || invoiceUrl style="margin: 8px; background-color: #1a1a1a; color: #ffffff; border: 1px solid #262626;") ⬇️ PDF
    if mobileAppUrl
      div(style="margin-top: 16px;")
        a(href=mobileAppUrl style="color: #71717a; text-decoration: none; font-size: 12px;") 📱 Ouvrir dans l'application mobile
